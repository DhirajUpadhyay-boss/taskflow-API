version: '3.8'

services:
  mongodb:
    image: mongo:7-jammy
    container_name: taskmanagement_mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
